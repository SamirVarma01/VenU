{
  "name": "venu-firestore-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx firebase-tools emulators:exec --config ../firebase.json --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1"
  }
}
