{
  "name": "venu-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "seed-schools": "node --env-file=.env seed-schools.mjs",
    "sync-concerts": "node --env-file=.env sync-concerts.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
